# `world/`

The world as data and simulation: terrain, deterministic generation, streaming
and persistence. No presentation, no UI.

Generation is a pure function of `(seed, world coordinates, generation version)`.
