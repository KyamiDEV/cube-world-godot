# `tools/`

Developer tooling that is not shipped game code.

| Dir | Contents |
|---|---|
| `probe/` | headless environment/capability probes (e.g. Voxel Tools verification) |
| `scripts/` | CLI run/check/test helpers |
| `local/` | machine-local overrides (gitignored) |
