# `ai/`

NPC and creature decision-making: behavior, navigation, perception, combat tactics.

AI reads world/entity state and emits intents. It never writes presentation state
and never bypasses the systems in `gameplay/`.
